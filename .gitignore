# Large SQLite databases (exceed GitHub's 100 MB file limit)
*.sqlite
*.db

# Allow the address lookup DB to be committed via Git LFS
!censu_app/data/address_geoid.sqlite

# R session artifacts
.Rhistory
.RData
.Rproj.user/
